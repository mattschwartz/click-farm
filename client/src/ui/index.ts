// UI module.
// Responsibility: React components that present game state and dispatch
// player actions. Never mutates game state directly. See core-systems.md —
// "State Management: Plain State Object + Immutable Tick".
export {}
