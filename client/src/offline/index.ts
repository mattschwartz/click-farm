// Offline calculator.
// Responsibility: compute state delta between close and open, walking the
// seeded Algorithm shift schedule segment-by-segment. See core-systems.md —
// "Offline Calculator".
export {}
